---
import { HeroSimpleWithProvider } from "../HeroSimple.tsx";
---

<div id="hero-section">
  <!-- 使用 client:load 而不是 client:only，这样 Astro 会先渲染初始 HTML，减少空白屏幕时间 -->
  <HeroSimpleWithProvider client:load />
</div>
